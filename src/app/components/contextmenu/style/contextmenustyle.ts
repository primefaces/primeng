
    import { BaseStyle } from 'primeng/base';

    const theme = ({dt}) => ``;

    const classes = {};

    const inlineStyles = {};

    export default BaseStyle.extend({
       name: 'contextmenu',
       theme,
       classes,
       inlineStyles
    });
    