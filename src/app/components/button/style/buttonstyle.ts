
    import { BaseStyle } from 'primeng/base';

    const theme = ({dt}) => ``;

    const classes = {};

    const inlineStyles = {};

    export default BaseStyle.extend({
       name: 'button',
       theme,
       classes,
       inlineStyles
    });
    